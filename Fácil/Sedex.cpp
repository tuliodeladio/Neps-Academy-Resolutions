#include <iostream>

using namespace std;

int main()
{
    int n,a,b,c;
    cin >> n >> a >> b >> c;

    if (n > a or n > b or n > b)
        cout << 'N';
    else
        cout << 'S';

    return 0;
    
}
